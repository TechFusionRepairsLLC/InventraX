# InventraX — Python Dependencies
# Install with:  pip install -r requirements.txt

# ── Core UI ──────────────────────────────────────────────────────────────────
PyQt5>=5.15.0

# ── Data & Export ─────────────────────────────────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.0          # Excel read/write (.xlsx)

# ── Charts ───────────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── Barcode / QR ─────────────────────────────────────────────────────────────
qrcode[pil]>=7.4.2        # QR code generation  (includes Pillow)
Pillow>=10.0.0            # Image handling

# ── Barcode Scanning (camera) — optional but recommended ─────────────────────
# If your device has a webcam and you want live camera scanning:
opencv-python>=4.8.0
pyzbar>=0.1.9             # Linux: also run  sudo apt install libzbar0
                          # macOS: also run  brew install zbar

# ── PDF Export — optional ─────────────────────────────────────────────────────
# Required only for  reporting.export_report_pdf()
reportlab>=4.0.0

# ── Database ─────────────────────────────────────────────────────────────────
# sqlite3 is built into Python — no install needed